node_modules/
*.class
target/
*.jar

# Production secrets — NEVER commit
deploy/.env
deploy/certs/
*.pem
*.key

# Build artifacts (regenerate via npm run build)
server/src/main/resources/webshop-static/
webshop/dist/
